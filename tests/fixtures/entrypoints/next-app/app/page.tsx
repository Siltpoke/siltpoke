// Fixture: Next.js App Router page — a framework-convention entrypoint file
// (app/page.tsx), detected by path convention regardless of what's exported.
export default function Page() {
  return "home";
}
