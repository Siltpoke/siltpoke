// Fixture: some real source so this is an actual (tiny) repo, unrelated to
// the opaque-scripts assertion (that no ep:script:* entry is emitted for
// framework-CLI / chained shell commands like "next dev" or "concurrently").
export function helper() {
  return 1;
}
